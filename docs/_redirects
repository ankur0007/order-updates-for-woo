# Clean URLs (/welcome serves welcome.html, etc.) are handled automatically
# by Cloudflare Workers static assets. Do NOT add "/path /path.html 200"
# rewrite rules here — Workers also redirects /path.html back to /path, so
# the rewrite creates an infinite redirect loop.
